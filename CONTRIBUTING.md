# Contributing Guide

このリポジトリに貢献してくれるみんなへ ✨  
コードやドキュメントを変更するときは、以下のルールに従ってください。

---

## 📝 コミットメッセージのルール

コミットメッセージには **prefix (type)** を必ず先頭に付けてください。  
これにより、履歴の可読性が上がり、変更内容が一目でわかります。

### よく使う type (prefix)

| type (prefix) | 意味 | 例 |
| --- | --- | --- |
| **feat:** | 新機能追加 | feat: ユーザー登録APIを追加 |
| **fix:** | バグ修正 | fix: 見積金額が保存されない不具合を修正 |
| **docs:** | ドキュメントのみの変更 | docs: READMEに環境構築手順を追記 |
| **style:** | コードスタイル変更（機能に影響なし） | style: インデント調整 |
| **refactor:** | 機能変更なしのリファクタリング | refactor: コントローラの共通処理を抽出 |
| **test:** | テスト追加・修正 | test: 契約更新処理のテストを追加 |
| **chore:** | ビルド・ツール設定など調整 | chore: npmパッケージを更新 |
| **perf:** | パフォーマンス改善 | perf: クエリ数を削減して高速化 |

### ルール
- 必ず `type: 内容` の形式で書く
- 英語・日本語どちらでもOK
- 複数の変更がある場合は、分けてコミットすること

---

## 🔧 コミットメッセージのチェック

このリポジトリでは、prefixがないコミットを防ぐために  
`.git/hooks/commit-msg` フック（または Husky）を設定しています。

コミット時にエラーが出た場合は、メッセージ先頭に正しい prefix を付けて再コミットしてください。

---

## 🛠️ 開発環境・ブランチ運用など
- `main` ブランチは常にデプロイ可能な状態を保ちます
- 新しい機能は `feature/◯◯` ブランチを切って作業します
- バグ修正は `fix/◯◯` ブランチを使います

---

不明点があれば、Issue や Pull Requestで相談してください！
